## Learn the power of IF-statement through this game

import random

user_input = int(input("Enter a value"))  # Convert the input to an integer
lucky_number = random.randint(0, 5)  # Generate a random number between 0 and 99

if user_input == lucky_number:  # Check if user_input is the same as the lucky_number
    print("Super rare! Want to play again?")
elif user_input > lucky_number:  # Check if user_input is greater than the random lucky number
    print("You won! Want to play again?")
else:
    print("You lose! Try again?")
